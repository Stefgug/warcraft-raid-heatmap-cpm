name: Deploy (placeholder)

on:
  workflow_dispatch:

jobs:
  containerize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Echo placeholder
        run: |
          echo "Build/push container to GHCR could go here."

